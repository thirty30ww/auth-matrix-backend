<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thirty.user.mapper.UserRoleMapper">
    <insert id="addUserRoles">
        INSERT INTO user_role (user_id, role_id)
        VALUES
        <foreach collection="roleIds" item="roleId" separator=",">
            (#{userId}, #{roleId})
        </foreach>
    </insert>

    <select id="selectRolesByUserId" resultType="com.thirty.user.model.entity.Role">
        SELECT
            role.*
        FROM user_role ur
        INNER JOIN role ON ur.role_id = role.id
        WHERE ur.user_id = #{userId}
        AND ur.is_delete = 0
        AND role.is_delete = 0
    </select>

    <select id="selectRoleIdsByUserIds" resultType="java.lang.Integer">
        SELECT role_id FROM user_role WHERE user_id IN
        <foreach collection="userIds" item="userId" separator="," open="(" close=")">
            #{userId}
        </foreach>
        and is_delete = 0
    </select>
</mapper>
